<!DOCTYPE html>
<html lang="en">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <title>Dropp Payment Callback</title>
    <style>
      * {
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Ubuntu,
          Cantarell, Noto Sans, sans-serif, BlinkMacSystemFont, "Segoe UI",
          Helvetica, Arial, sans-serif, "Apple Color Emoji", "Segoe UI Emoji",
          "Segoe UI Symbol";
      }
      html {
        padding: 30px;
        background-color: #def8ff;
      }
      body {
        padding: 10px 60px;
        background-color: #fff;
        border-radius: 10px;
        min-height: 400px;
      }
      h1 {
        margin-bottom: 7px;
      }
      .title-bar {
        margin-bottom: 50px;
      }
      .title {
        border-bottom: 3px solid black;
      }
      .subtitle {
        font-size: 90%;
        color: #666;
      }
      li {
        padding: 10px 0;
        list-style-type: "~  ";
      }
      a {
        padding: 5px 10px;
      }
      a,
      a:visited {
        color: black;
        text-decoration: none;
        border-bottom: 1px dotted black;
        background-color: #fff;
      }
      a:hover {
        color: black;
        text-decoration: none;
        background-color: #def8ff;
      }
      .return-buttons {
        margin-top: 20px;
      }
      .return-buttons button {
        margin-right: 10px;
      }
      .payment-data {
        margin-top: 20px;
        white-space: pre-wrap;
        word-wrap: break-word;
      }
    </style>
  </head>
  <body>
    <div class="title-bar">
      <div class="title">
        <h1>Dropp Payment Callback</h1>
      </div>
    </div>

    <div class="payment-data">
      <pre id="paymentResponse"></pre>
    </div>

    <script>
      function displayPaymentData(data) {
        var paymentResponseElement = document.getElementById("paymentResponse");
        paymentResponseElement.textContent = JSON.stringify(data, null, 2);
      }

      var queryParams = new URLSearchParams(window.location.search);
      var paymentResponse = queryParams.get("paymentResponse");

      if (paymentResponse) {
        var decodedPaymentResponse = decodeURIComponent(paymentResponse);
        var paymentResponseData = JSON.parse(decodedPaymentResponse);
        displayPaymentData(paymentResponseData);
      }
    </script>
    <a href="/">Return Home</a>
  </body>
</html>
